---
# defaults file for ansible-role-nvidia-docker
docker:
  install: yes
  compose:
    install: yes
    version: 1.28.2

nvidia:
  skip_reboot: yes
  driver:
    install: yes
    from_cuda: yes
  container:
    install: yes

compose:
  jupyter_hub:
    prepare: yes
    ip: "0.0.0.0"
